{{/* output a dict of tags; lookup by slug to get display title */}}
{{ $tags := dict }}
{{ range $tag, $content := .Site.Taxonomies.tags }}
	{{ $slug := .Page.RelPermalink | path.Base }}
	{{ $tags = merge $tags (dict $slug .Page.Title) }}
{{ end }}
{{ $tags | jsonify (dict "indent" "  ") }}
